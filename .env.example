# ComfyUI working directory — where validated JSON files are transferred to
COMFYUI_OUTPUT_PATH=/path/to/comfyui/input

# App settings
APP_HOST=0.0.0.0
APP_PORT=8080
APP_RELOAD=false

# Database
DATABASE_URL=sqlite:///data/aivp.db
